부운영자용 운영권한을 추가하시려면 다음과 같은 절차를 따르세요.
먼저 기존에 있던 txt파일들중 하나를 복사해서 원하는 규칙 이름으로 바꿔줍니다.
(한글이나 영어 아무것도 상관없습니다)
여기서는 복사한 파일의 이름을 "킥만 가능.txt" 로 설정했습니다.
txt파일을 열어보면 대충 이런 내용이 있습니다.
ex)
1 // 귓속말 추적권
1 // 명령어 추적권
1 // 운영관련 메세지 수신권
1 // 부영자용 채팅 사용권 (/말 , /say)

맨 앞에있는 숫자를 0으로 바꾸면 해당 권한을 사용할 수 없게 됩니다.
0 // 부영자용 채팅 사용권

이렇게 바꾸면 이 권한이 적용된 사람은 /말 명령어를 사용하지 못하게 되는거죠.
운영자분께서 원하시는대로 내용을 변경하시기 바랍니다.

권한규칙 설정이 다 되었으면 규칙을 RconController.ini에 연결해주어야 합니다.
RconController.ini에서 Auth_Profile 뒤에 숫자를 붙입니다.
Auth_Profile1=킥밴_금지.txt
Auth_Profile2=출두만 가능.txt
Auth_Profile3=기본 운영권(추천).txt
이렇게 되어있다면

Auth_Profile4=
이런식으로 적어줍니다.
*주의: 숫자는 중간에 빠짐없이 1부터 순서대로 쓰셔야 합니다.

그리고 =표시 뒤에는 권한 규칙이 들어있는 파일 이름을 적어줍니다.
ex) Auth_Profile4=킥만 가능.txt
이렇게 적어주면 연결이 된 겁니다.
* 확장자는 어느것으로 해도 상관없습니다.

자, 이제 권한번호 4번이 생겨났습니다.
고정 부운영자에게 해당 권한을 주려면
ex) SubAdmin1=CoolGuy|password|127.0.0.1|0|

해당 유저의 맨 마지막에 있는 0을 (0번은 운영자와 같은 권한을 의미합니다.) 다른 숫자로 바꿔주시면 됩니다.
여기서는 새로만든 4번 권한을 주도록 하겠습니다.
ex) SubAdmin1=CoolGuy|password|127.0.0.1|4|

이제 CoolGuy가 /부운로그인 password를 입력해서 로그인하면 자동으로 4번 권한을 주게 설정되었습니다.

그리고 이 권한번호는 AUTO_AUTHORITY 부분에도 적용할 수 있습니다.
기본적으로 ini파일에는 다음과 같은 내용이 있습니다.
ex) AUTO_AUTHORITY=3

/부운 명령어로 권한을 줄때 자동으로 3번 권한을 주도록 한 겁니다.
이걸 새로 만든 권한으로 변경하려면
ex) AUTO_AUTHORITY=4

이렇게 변경해주면 됩니다. 간단하죠?
또한 현재 로그인되어 있는 부운영자의 권한을 변경할때도 이 권한번호를 사용할 수 있습니다.


어려웠나요?
기타 자세한 궁금증은 cafe.daum.net/Coolpdt에서 물어보세요.
자세하게 답변해 드리도록 하겠습니다.